# Validation fixtures

Positive compilation evidence is generated by `conftest.py` and explicitly
classified as `synthetic-test-fixture`. Negative cases clone and mutate that
fixture in memory. Test evidence is never loaded by the runtime model or used as
SAP evidence.

